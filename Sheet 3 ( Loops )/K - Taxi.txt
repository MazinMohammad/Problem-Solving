K - Taxi


Solution:





My Solution (wrong if you can't seperate group members):
#include <iostream>
#include <cmath>
using namespace std;

int main()
{
   int n, sum=0;
   cin >> n;
   int *ptr = new int [n];
   for (int i = 0; i < n; i++)
   {
      cin >> ptr[i];
      sum+=ptr[i];
   }
   cout << ceil(sum/4.0);
}
